<h3>Facebook Post</h3>
<%= form_for @post do |f| %>
	<div class="field">
		<%= f.label :body %>
		<br>
		<%= f.text_area :body , :rows => 5 , :cols => 40 %>
	</div>
	<div class="actions">
		<%= submit_tag t(:post) %>
	</div>
<% end %>
